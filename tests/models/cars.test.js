import test from 'tape';
import * as cars from '../../src/models/cars';

function setUp() {

}

test('Cars ', nest => {
  nest.test('Gets a List of Cars', assert => {
    const data = setUp();
    assert.ok(false, 'Returns a list of cars');
    assert.end();
  });

});
